# Filename patterns (gitignore-style globs, matched against each output file's basename) of
# pipeline output files that ica_tools/compare_tests.py downloads and diffs between two test
# runs. Add a pattern here for any output file whose content should stay stable across changes
# that aren't supposed to affect pipeline outputs (e.g. performance/memory fixes). Leave out
# large files (matrices, BAMs, etc.) that aren't practical to diff directly.
*.scRNA_metrics.csv
