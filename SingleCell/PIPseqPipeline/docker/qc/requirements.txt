# Additional pip packages for guide assignment
# pandas, numpy, scanpy, anndata, matplotlib, scipy are installed via conda (see Dockerfile).
# crispat (and its dependencies, e.g. pyro-ppl) are installed separately via git clone + pip install (see Dockerfile).
# Add any other pip-only packages here if needed
